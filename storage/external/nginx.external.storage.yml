apiVersion: v1
kind: Pod
metadata:
  name: volpod
spec:
  volumes:
  - name: data
    persistentVolumeClaim:
      claimName: one-gig
  containers:
  - name: nginx
    image: nginx
    volumeMounts:
    - mountPath: /data
      name: data