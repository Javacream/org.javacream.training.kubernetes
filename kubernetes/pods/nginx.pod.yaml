kind: Pod
apiVersion: v1
metadata: 
  name: nginx
spec:
  containers:
    - name: nginx-container
      image: nginx
      resources:
        limits:
          memory: "30Mi"
          cpu: "200m"
        requests:  
          memory: "20Mi"
          cpu: "100m"
