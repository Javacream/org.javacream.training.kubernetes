apiVersion: v1
kind: Service

metadata:
  name: web-nodeport

spec:
  type: NodePort

  selector:
    chapter: services
  ports:
  - protocol: TCP
    port: 9000
    targetPort: 8080
    nodePort: 30090